# 《凌晨四点的宠物医院》原型 UI 与交互基线

状态：当前 PC／手机已确认设计的持久基线。后续章节默认复用，除非 Lisa 明确要求改变。

这份文件记录“必须保持的体验”和“当前校准值”。运行实现仍以 `index.html`、`app.js`、`game-data.js`、`styles.css` 与实际素材为准；任何有意改变基线的修改，都必须同时更新本文件和视觉 QA。

## 1. 使用方式与真相层级

1. Lisa 在当前任务中的明确要求优先。
2. 本文件记录已经确认、后续应继承的设计意图。
3. 当前代码和素材是运行事实；若与本文不一致，先通过同视口检查判断是旧文档还是实现回归，不要擅自选择一边。
4. `design-qa.md` 是历史问题和验证证据，不得把已经废弃的旧数值重新带回代码。
5. 人物视觉的额外规则见工作区 skill：`pet-hospital-portrait-direction`。

基准检查视口：PC 1280 × 720 CSS px；手机竖屏 393 × 852 CSS px。遇到用户真机问题时，再增加用户的实际内容视口，不用浏览器外壳尺寸替代页面视口。

## 2. 共享产品骨架

- 游戏始终是单页、满视口舞台：`.game-shell` 使用 `100vw × 100dvh`，页面不滚动，舞台裁切内容。
- 场景背景默认 `center / cover / no-repeat`，切场只改变背景和当前交互层，不改变 HUD、弹窗、对话框的结构。
- 所有屏幕复用同一套 HUD、对话、选择、证物、记录、设置和提示组件；后续章节不得复制一套外观近似但数值不同的新组件。
- 主断点是 `820px`：大于 820px 为 PC／大屏基线；不超过 820px 进入移动布局；不超过 820px 且竖屏进入专门的询问与横向场景布局。
- 不超过 1024px、横屏且为粗指针设备时显示竖屏提示，不把手机横屏硬塞进 PC 布局。
- 主色与纸张色由 `:root` 变量控制：`--green #183f36`、`--green-2 #296658`、`--green-3 #dce8e3`、`--paper #f7f5ef`、`--paper-2 #ebe9e2`、`--ink #202624`、`--muted #66716d`、`--blue #7ea7cf`。
- 字体为中文无衬线 UI 搭配宋体系标题。不得在后续页面引入一套新的字体气质。

当前流程的 UI 顺序是：封面 → 凌晨来电与三选一回答 → 案件标题 → 医院外景／等候区 → 秦昭询问 → 第二诊室搜证 → 等候区选人 → 唐宁、苏青、林夏自由顺序询问 → 隔离间搜证 → 再次询问林夏 → 血迹鉴定 → 唐宁证言对质与证物出示 → 03:42—03:46 时间线反转 → 第二轮结束。后续等候区第三轮调查从这套组件向后延伸。

### 2.1 封面、标题与进入场景

- 封面和案件标题使用左侧文字、右侧环境主体的叙事构图，并用从左深到右浅的遮罩保证文字可读；不在封面叠加人物立绘。
- PC 封面／标题文字区左侧为 `clamp(36px, 8vw, 126px)`，垂直中心附近；封面标题使用 `clamp(48px, 6.4vw, 92px)`，案件标题使用 `clamp(42px, 4.8vw, 68px)`。
- 手机文字区左右留 26px，宽度为 `100% - 52px`，封面和标题主标题统一收为 45px。
- 进入按钮沿用绿色半透明描边按钮，封面按钮宽 190px、最小高 52px；后续章节入口继续沿用 `.cover-button` 或 `.primary-button`，不另造视觉体系。
- 同类入口统一使用“进入＋地点／环节”（例如“进入第二诊室”“进入隔离间”“进入质疑”），不混用“前往”“进入现场”等同义动词。调查出口统一为“完成调查”，询问出口统一为“完成询问”；轮次出口可写成“完成第一轮询问”。
- 电话场景使用专门背景且不显示人物立绘；案件标题页隐藏 HUD；进入医院后才展示正常 HUD。
- 搜证前的场景介绍使用 `.search-briefing`：PC 左侧 `clamp(28px, 6vw, 92px)`、底部 `clamp(40px, 10vh, 120px)`、最大宽 570px；手机保持同一信息层级，不改成独立说明页。

## 3. HUD 与固定层级

PC：

- `.hud` 顶部内边距 `24px 28px`。
- 场景名 18px，时间／进度 12px；左侧短横线 34 × 2px。
- 右侧按钮最小高 42px、圆角 22px，显示图标和文字；证物数量使用独立圆形徽标。

手机：

- HUD 内边距 14px；场景名 15px。
- 操作按钮 40 × 40px，只显示图标；证物数量徽标叠在右上。
- HUD 永远高于场景与人物，但低于弹窗、遮罩和 toast。

## 4. 对话框与推进交互

PC：

- `.dialogue`：底部 22px；宽 `min(1180px, 100% - 48px)`；最小高 174px；内边距 `55px 70px 34px`。
- 姓名牌：左 34px、上 -30px、最小宽 246px、内边距 `14px 28px`。
- 正文 `clamp(18px, 1.7vw, 23px)`，行高 1.78。
- 三选一选择面板与对话框使用相同宽度、底部位置和最小高度，不能突然换成独立卡片页。

手机：

- 对话框底部 14px；宽 `100% - 28px`；最小高 205px；内边距 `51px 24px 36px`。
- 姓名牌：左 17px、上 -28px、最小宽 190px、内边距 `12px 20px`。
- 正文 17px、行高 1.66；推进提示位于右下。
- 选择面板继续使用同一占位，三个选项每项最小高 48px。

推进规则：

- 点击对话框、明确的推进按钮，或点击非交互场景区域，都只推进一行。
- 场景手势移动超过 12px 视为拖拽／滑动，不推进文字。
- HUD、弹窗、选择、表单、按钮、链接、搜证热点等交互区域不得触发场景推进。
- 旁白隐藏姓名牌；人物切换时只显示当前说话者的一张立绘。

## 5. 对话立绘基线

共同规则：

- `.speaker-portrait` 位于独立 `.character-layer`，人物保持完全不透明；需要压暗时只压暗背景。
- 单帧素材使用 `background-size: contain` 和底部居中，严禁分别拉伸宽高。
- 下缘必须藏在对话框后或柔和淡出，不能露出硬切边。
- 许知衡与江跃因素材朝向使用水平翻转；其他人物不翻转。
- 当前说话立绘左右：许知衡在左；江跃、秦昭、唐宁、苏青、林夏在右。
- 许知衡必须高于三名嫌疑人；嫌疑人之间保持唐宁最高、林夏其次、苏青最低。身高主要通过头顶与肩线表达，不通过明显缩小头部表达；江跃、秦昭沿用各自既有校准，不强行插入嫌疑人的连续排序。
- 同视口可见头部尺度差异控制在约 10% 内；素材画布留白不同，最终以视觉比较为准。
- 唐宁使用 `neutral/anxious`，苏青使用 `neutral/guarded`，林夏使用 `neutral/troubled`；表情只在本人明确情绪转折台词切换，普通事实回答默认中性。

### 5.1 PC 当前校准值

| 人物 | 宽 | 高 | bottom | 横向锚点 |
| --- | ---: | ---: | ---: | --- |
| 许知衡 | 49.5vh | 99vh | -9vh | 左侧共享锚点 |
| 江跃 | 43vh | 88vh | -4vh | 右侧共享锚点 |
| 秦昭 | 48vh | 72vh | 8vh | 右侧共享锚点 |
| 唐宁 | 69.3vh | 104vh | -17.5vh | 右侧共享锚点再向内补偿 8vh |
| 林夏 | 57.5vh | 87vh | -5vh | 右侧共享锚点 |
| 苏青 | 51.5vh | 77vh | -3vh | 右侧共享锚点 |

PC 共享锚点：左／右均为 `max(18px, calc(50% - 590px))`。唐宁的特殊右锚点是 `max(18px, calc(50% - 590px - 8vh))`。

### 5.2 手机当前校准值

| 人物 | 宽 | 高 | bottom | 横向锚点 |
| --- | ---: | ---: | ---: | --- |
| 许知衡 | 39dvh | 78dvh | -1.7dvh | left -4% |
| 江跃 | 35vh | 72vh | -2vh | right -4% |
| 秦昭 | 42vh | 63vh | 2vh | right -4% |
| 唐宁 | 55.3dvh | 83dvh | -6.9dvh | right -12%（补偿源画布偏心） |
| 林夏 | 47.3dvh | 71dvh | -3dvh | right -4% |
| 苏青 | 45.3dvh | 68dvh | -4.4dvh | right -4% |

这些值与当前素材绑定。替换人物图、扩画布或改变透明留白后，必须重新测量并做同视口视觉比较，不能机械沿用。

## 6. 询问／话题选择布局

询问界面以秦昭版为唯一结构模板；唐宁、苏青、林夏只做素材画布补偿，不创建不同构图。

PC：

- 中央半身像 `.topic-person`：水平居中、底部 10vh、52 × 78vh、2:3 容器。
- 底部使用透明 mask 柔和淡出；脸部不能被按钮遮挡。
- 四个话题卡位于左右两侧：左／右 7%，上排 31%，下排 61%；卡宽 `min(300px, 24vw)`、最小高 78px。
- 连接线只在 PC 显示；“完成询问”按钮居在底部中间，最小 228 × 50px、底部 22px。

手机竖屏：

- 场景背景位置为 `58% center`，保证人物与环境构图。
- 中央人物容器统一为：top 68px；宽 `min(78vw, 320px)`；高度自动；2:3；底部柔和淡出。
- 话题卡改为底部两列紧凑网格：左右 12px、底部 72px、间距 8px、内边距 11px；单卡最小高 52px。
- “完成询问”按钮底部 14px、最小 190 × 46px。
- 唐宁和林夏因源图留白使用 `background-position: center 10px; background-size: auto 120%`；苏青使用 `auto 110%`；不得因此改变共享容器与话题卡位置。

## 7. 搜证场景布局

搜证和选人都必须使用同一个 16:9 场景坐标层：背景图、人物／物体和点击热区一起缩放、一起横移。绝不能按视口算热区、却用另一套比例裁背景。

PC：

- `.interaction-canvas` 水平垂直居中，16:9；宽 `min(100vw, 100dvh × 16/9)`，背景 cover。
- 搜证提示默认隐藏；鼠标移动后热点才进入 armed 状态，悬停显示高亮和标签。
- “完成调查”按钮位于右下，最小 148 × 44px；三件必查证物未齐时禁用。

手机竖屏：

- `.interaction-scroll` 位于 top 68px、bottom 70px，可横向滑动、不可纵向滚动，`touch-action: pan-x`。
- 16:9 场景画布高度为 `min(65dvh, 100dvh - 150px)`，最小高 360px，宽度由 16:9 自动得出，因此会超出窄屏。
- 首次进入默认滚到场景中部；离开再回来保留 `scrollLeft`。
- 持续显示提示“左右滑动查看完整场景 · 点击可疑位置”，提示位于底部 61px。
- “完成调查”按钮底部 12px、水平居中、最小 140 × 42px。

第二诊室当前热点坐标（均相对 16:9 画布）：

| 热点 | left/top | width/height | 说明 |
| --- | --- | --- | --- |
| 检查台 | 39.5% / 52% | 31% / 43% | 场景事实 |
| 器械架 | 17.5% / 49% | 21% / 39% | 必查 |
| 打印机／报告 | right 0.5% / 38% | 17% / 28% | 必查 |
| 诊室门 | 70.5% / 17% | 12% / 51% | 场景事实 |
| 变形缝 | 20% / 53% | 16% / 23% | 查过器械架后才出现，必查 |

离场条件是器械架、报告、变形缝三项齐全。不要用新 UI 自动替玩家生成“死亡时间已缩小”等结论。

隔离间当前热点坐标（均相对同一 16:9 画布）：

| 热点 | left/top | width/height | 说明 |
| --- | --- | --- | --- |
| 隔离门／平面图 | 2.5% / 7% | 22% / 72% | 必查，获得医院平面图 |
| 治疗台 | 28% / 46% | 39% / 44% | 必查，获得使用过的治疗用品 |
| 住院卡 | 84% / 52% | 10% / 25% | 必查，获得空白住院卡 |
| 观察摄像头 | 86% / 1% | 12% / 18% | 必查，获得隔离间录像 |

隔离间四项均为必查，全部完成后才开放“完成调查”。手机初始仍滚动到画面中部，门／平面图位于左侧，住院卡和摄像头位于右侧，必须通过横向滑动查看完整场景。

等候区第三轮搜证复用同一 16:9 场景坐标层，使用 `waiting-area-investigation-v1.jpg`，画面必须同时看得到原座位上的猫包与水瓶、第二诊室入口、三个临时留观笼和桌面直播平板，避免热点文案与背景物体不符。

| 热点 | left/top | width/height | 说明 |
| --- | --- | --- | --- |
| 苏青原先的座位 | 1.5% / 43% | 23% / 35% | 补充调查 |
| 临时留观区 | 57% / 45% | 22% / 27% | 补充调查 |
| 直播平板 | 43.5% / 65% | 20% / 17% | 必查，进入三段回放查看 |
| 第二诊室入口 | 79% / 13% | 14% / 57% | 必查，获得留观动物的反应 |

三段直播全部查看后才获得《完整直播录像》；第二诊室方向和直播回放两项完成后才开放“完成调查”。回放时间段选择继续占用标准底部选择区，不创建全屏独立应用界面；手机改为单列并允许选择区内部滚动。

## 8. 等候区选人布局

- 选人界面复用上述 16:9 可平移场景，不把三个人拆成独立卡片列表。
- 热区必须与背景人物共用场景坐标。唐宁 `left 6%, top 29%, width 17%, height 57%`；苏青 `42%, 31%, 14%, 50%`；林夏 `75%, 34%, 18%, 53%`。
- 每个可选人物显示姓名、身份、与案件的关系、话题进度／完成状态，不得只显示姓名。
- PC 通过悬停显示人物信息；手机信息常显。
- 手机选人场景底部为完成按钮预留 106px；滑动提示底部 62px；“完成第一轮询问”按钮底部 10px、居中、最小 190 × 44px。
- 未完成三人询问时完成按钮禁用；三人的先后顺序由玩家自由选择。

## 9. 证物获得与证物册

证物获得层：

- PC 使用左右两栏：图片区与说明区；整体最大宽 910px、最小高 440px，图片区最小高 440px，说明内边距 44px。
- 手机改为单栏并允许内部纵向滚动；图片区固定 240px，说明内边距 26px，整体最大高 `100vh - 40px`。
- 图片未解码时显示明确加载态；加载后再淡入，不闪回空白或背景色。

证物册：

- PC 弹窗最大宽 1120px；内容最大高 `100vh - 150px`。
- PC 证物浏览为左侧两列证物卡、右侧大图说明；网格与详情最小宽分别约 380px 和 360px，详情大图高 300px。
- 手机弹窗宽 `100% - 24px`；证物卡改为单列，详情移到下方并用上边框分隔。
- 已补充的信息（例如死亡时间）要更新原证物说明，不新建结论型证物。

## 10. 弹窗、设置与记录

- 通用弹窗居中，最高 `100vh - 64px`；遮罩独立覆盖场景；Escape、关闭按钮和遮罩点击均可关闭。
- 对话记录保存当前流程最近 100 行，PC 使用 130px 说话人列 + 正文；手机保持可读并允许弹窗内部滚动。
- 设置保持可实际操作：声音开关、总音量、文字速度、字号；设置值写入 localStorage。设置中不提供音效或 BGM 试听，质疑 BGM 固定使用“法庭序曲”。
- 手机设置项改为单列，控件左对齐；不得为了视觉简化删除功能。

## 10.1 证言对质与出示证物

- 进入证言质疑前先播放一次全屏“证言质疑 / CROSS-EXAMINATION”标题动效和低音量木槌提示音；必须由玩家点击“进入质疑”后才开始逐句质询，不得倒计时自动进入。该转场只用于提示玩法切换，不替代后续对话界面。
- 正式对质复用普通剧情的标准对话框、背景和唐宁既有响应式立绘标尺，不再使用独立的大卡片或纵向证言列表。对话框同屏只显示当前一句证言，说话人栏同时显示当前序号、总句数和追问状态。
- 对话框底部固定放置上一句／下一句、位置短线和“追问／质疑”操作；左右按钮、键盘方向键和手机横向滑动均可循环切换，短线同时标出当前位置及已经追问的句子。
- PC 保持已确认的普通对话框和唐宁 PC 立绘比例；手机保持已确认的竖屏对话框、唐宁位置及大小，“追问／质疑”并列且至少 48px 触控高度，页面本身不滚动。
- “追问”只针对当前句并进入该句追问对话；返回后保留当前句及“已追问”状态。“质疑”只针对当前句打开证物选择层。
- 进入证言对质时固定切换到“法庭序曲”，以明显低于常规剧情 BGM 的音量平滑淡入；离开本轮对质并进入第二轮结算时恢复常规剧情 BGM。开关门声使用低音量真实录音，不使用合成门声。对话推进以 `pointerdown` 即时响应，推进音使用单个预加载播放器并在连续点击时截断重播，不允许多个“咚”声排队叠加；询问话题完成不播放额外提示音；调查提示音只在玩家点中搜证热点时播放，获得证物继续使用独立证物音。音量与静音仍统一受设置控制。剧情音效使用预加载并复用的实体音频对象，与 BGM 共用 HTML Audio 路径，不依赖单独的 Web Audio 合成链路。
- 证物选择层复用证物图、编号和名称。PC 三列，手机单列且弹层内部滚动；错误证物必须给出克制提示并返回原问题，不能重置剧情进度。
- 第一处矛盾必须由玩家在证言 3 出示证物。《使用过的治疗用品》和《隔离间录像》都能直接反驳“没有治疗”；选择用品后继续要求录像证明操作者，直接选择录像则进入录像揭示，不重复要求同一证物。
- 推理选择必须由玩家用“39.7℃实时照片”排除语音提前录制；之后依次出示《叠放的两份报告》和《隔离间录像》，亲自得出 03:42—03:46 的作案时间与唐宁、林夏的不在场证明。
- 苏青终局对质继续使用同一个证言对话框，不新增纵向证言卡：第一阶段在证言 2 依次用《变形的器械架》《医院平面图》《留观动物的反应》击破“医院一直安静”；第二阶段先从三段直播中选择 03:51 以后证明收音与动物反应，再质疑“从头到尾实时”；第三阶段在“奶糖没有进入第二诊室”出示更新后的《奶糖的猫毛》，并通过新形成的变形缝锁定撞击时间；最后出示《叠放的两份报告》证明动机。
- 苏青每个新证言阶段可重置该阶段的追问短线，但不得清空已获得证物、直播查看进度或案件推理事实。终局认罪结束后恢复常规剧情 BGM，再进入案件结束、“完”画面和片尾。

## 11. 资源、切场与发布完整性

- 背景使用高质量轻量格式；封面和下一场景优先加载，其余背景在进入游戏后按浏览器空闲时机串行预载，不能在首屏同时抢占全部背景下载。人物立绘保持预加载；证物图在浏览器空闲时预加载。
- 打开证物时先显示明确加载态，图片解码后再淡入。
- 打开等候区人物询问时，如果背景没有改变，不对同一背景重复播放黑色切场动画。
- 表情变化替换完整单帧素材，不叠加第二张人像，也不随机轮换表情。
- 新立绘和证物图必须透明边缘干净、清晰，并保持角色身份、服装、姿态和风格一致。
- `scripts/copy-static-assets.mjs` 是上线资源清单；每次新增或更换被 `game-data.js`、`app.js` 引用的静态素材时，必须同步加入清单。不能只在本地开发环境可见。
- 每次改动完成后运行 `npm run build`，确认 `dist` 中存在所有运行时引用的资源。

## 12. 后续功能如何复用现有系统

- 新调查地点：继续使用 `interaction-scroll + interaction-canvas + hotspots`，为新场景单独定义相对热点坐标。
- 新询问人物：继续使用秦昭的话题屏结构和现有对话框，只新增人物数据、立绘校准和明确的表情切换。
- 新一轮嫌疑人选择：继续使用 16:9 场景坐标层与人物热区；不要回退成与场景脱节的卡片列表。
- 新证物：继续使用既有获得层和证物册；原始事实与玩家推理结论严格分开。
- 新剧情段落：继续通过 `setStage`、`setHud`、`runDialogue` 和现有完成回调组织，避免另建无法复用历史记录、设置或推进交互的页面系统。
- 新证言／推理：继续使用标准对话框内的逐句证言操作、对话选择与证物选择层；题目数据独立配置，错误反馈返回当前问题，不清空已经完成的追问或搜证状态。

## 13. 每次修改后的最低验证矩阵

1. 在 1280 × 720 PC 和 393 × 852 手机竖屏各检查一次被修改的界面。
2. 即使只改一个断点，也要检查另一个断点没有被连带破坏。
3. 对话相关改动检查人物、旁白、三选一、对话框点击、场景点击、滑动不误推进、HUD 与弹窗不误推进。
4. 立绘相关改动检查中性和所有实际使用的表情、PC 对话、PC 询问、手机对话、手机询问，并与许知衡／江跃／秦昭同视口比较。
5. 搜证或选人改动检查 PC 点击区、手机横向滑动、热区随画面移动、提示、禁用状态和完成按钮。
6. 证物相关改动检查首次加载、获得层、证物册、无证物空状态，以及 PC／手机两种布局。
7. 证言／推理相关改动检查前后循环切换、键盘方向键、手机滑动、当前句追问状态、当前句质疑、错误证物返回、正确证物链、错误推理返回及专属 BGM 切换。
8. 运行生产构建和 `git diff --check`，检查最终差异不含临时 QA 入口、调试状态或遗漏素材。

代码检查不能替代视觉检查。人物比例、裁切、遮挡、淡出、热点对齐和真机可读性都以相同视口下的实际画面为最终标准。
